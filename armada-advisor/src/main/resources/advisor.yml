advisor:
  cycleRate: 5000
  store:
    ttl: 10000
    pathFormat: 'armada/nodes/${node_id}/health'
    zookeeper:
      connectionString: localhost
      retryPolicy:
        exponentialBackoff:
          timeout: 3000
          retries: 3